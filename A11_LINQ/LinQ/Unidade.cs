using System;
using System.Collections.Generic;
using System.Text;

namespace TP06_Collections
{
    // Slide 5 - enum
    public enum Unidade
    { EST_SETUBAL = 210, EST_BARREIRO = 262, IPE = 269}

    enum Mes : byte
    { Jan, Feb, Mar, Apr, May, Jun, Jul, Aug, Sep, Oct, Nov, Dec };

    enum Dia : byte
    { SEG=2, TER, QUA, QUI, SEX, SAB, DOM}
}
